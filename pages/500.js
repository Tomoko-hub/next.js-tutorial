import utilStyles from "../styles/utils.module.css";

export default function Custom505(){
    return(
        <div>
            <h1 className={utilStyles.heading2Xl}>Can not found page.</h1>
        </div>
    );
}